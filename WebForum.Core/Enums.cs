namespace WebForum.Core
{
    public enum UserModelType
    {
        Full,
        Short,
    }

    public enum IdType
    {
        User = 1,
        entity = 2,
    }

    public enum DeleteType
    {
        NoVisible,
        Full
    }
}
