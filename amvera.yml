meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  context: WebForum
  dockerfilePath: Dockerfile
  skip: false
run:
  persistenceMount: /data
  containerPort: 8080
